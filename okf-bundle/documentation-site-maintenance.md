---
type: Reference
title: Documentation site maintenance
description: Canonical maintenance for docs.page and the generated TypeDoc API reference.
tags: [okf, documentation, docs-page, typedoc]
timestamp: 2026-08-22T00:00:00Z
---

# Documentation site maintenance

Single source for **user-facing documentation site** maintenance: [docs.page](https://docs.page)
content under `docs/` plus root `docs.json`, and the generated core API reference.

**Policy:** [OKF documentation and commit policy](documentation-policy.md).

<a id="docsjson"></a>

## docs.json

When adding or renaming user docs pages:

- Add or update sidebar entries in `docs.json` in the same change as `docs/**`.
- Paths in the sidebar are docs.page routes (for example `/ad-formats`), not GitHub file paths.
- Run the canonical prose and link checks owned by
  [validation checklist § lint](testing/validation-checklist.md#lint-and-formatting) and
  [§ docs.page link check](testing/validation-checklist.md#docs-page-link-check).

<a id="retired-routes"></a>

### Retired routes

This section is the single owner of retired-route handling.

- docs.page does **not** currently honour the `docs.json` `redirects` map: retired paths return
  404 on previews and on docs.page itself. Keep the map (old path → current route) as the record
  of intent, in sync when renaming or removing user-facing routes.
- Every retired path in that map keeps a stub page under `docs/` (for example
  `docs/displaying-ads.mdx`): a frontmatter `title`, a frontmatter `redirect` to the new route,
  and one or two sentences linking to the new page. The visible link must work without the
  redirect. Stubs stay out of the sidebar.
- Before claiming any redirect works, confirm it with `curl -sI <preview-url>/<old-path>` against
  the branch preview and check for a 3xx `location` header, not only a 200.

<a id="api-reference"></a>

## Generated API reference

The core TypeScript API reference is generated by TypeDoc 0.28 from `packages/core`:

- Root `typedoc.json` owns entry points, strict warning validation, navigation, output, and the
  hosted base URL contract.
- `reference-api-readme.md` owns the landing page and public adapter-package discovery;
  `docs/reference.css` owns TypeDoc presentation.
- `.github/workflows/deploy-api-reference.yml` publishes to
  `https://invertase.github.io/react-native-google-mobile-ads/`; repository Pages source must be
  **GitHub Actions**.
- `docs.json` owns the external Reference API tab. The retired curated reference route
  follows [retired routes](#retired-routes).

The generated site is core-only. Public scoped mediation adapters remain discoverable from the
landing page; `packages/_template` is private and excluded. Commands and forbidden alternatives
are owned by [agent command policy](testing/agent-command-policy.md#canonical-registry);
applicability and evidence are owned by
[validation checklist § API reference](testing/validation-checklist.md#api-reference).

## Related

- User docs live in `docs/`. Integrator agent steering ships in `packages/core/AGENTS.md` (npm); root `AGENTS.md` is the maintainer path and redirects integrators there. Maintainer/agent knowledge lives in `okf-bundle/` — [documentation policy](documentation-policy.md).
- CI docs job: `.github/workflows/docs.yml` — [§ lint](testing/validation-checklist.md#lint-and-formatting)
  (spellcheck and link check in CI; markdown check is local).
- TypeDoc and source-TSDoc validation follows
  [validation checklist § API reference](testing/validation-checklist.md#api-reference).
