/**
 * TODO: replace with format smoke specs against RNGoogleMobileAdsExample testIDs.
 * Kept skipped so `wdio` config can boot without a device session for scaffold validation.
 */
describe('Appium scaffold placeholder', () => {
  it.skip('device smoke lands when format specs are added', async () => {
    // Intentionally skipped — no emulator/simulator required for this scaffold.
  });
});
